import React from 'react';

import { Routes } from 'react-router-dom';
import Route from './Route';

const RoutesIndexer: React.FC = () => (
  <Routes>{/* <Route index component={<Home />} /> */}</Routes>
);

export default RoutesIndexer;
