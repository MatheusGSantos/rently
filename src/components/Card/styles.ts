import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  background-color: yellow;
  display: flex;
  padding: 1.5rem;

  #card-body {
    flex: 1;
    padding-left: 1rem;
  }
`;
